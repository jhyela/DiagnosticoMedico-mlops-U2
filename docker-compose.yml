version: '3.8'
services:
  frontend:
    build: frontend
    ports:
      - 8501:8501
    depends_on:
      - backend
    volumes:
      - .:/app
  backend:
    build: backend
    ports:
      - 8000:8000
    volumes:
      - .:/app